<fw:AcrylicWindow x:Class="NoteTaker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fw="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
        xmlns:local="clr-namespace:NoteTaker"
        mc:Ignorable="d"
        Title="" Height="110" Width="600" TintOpacity="0.5" TintColor="LightGray">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" HorizontalAlignment="Stretch">
            <TextBlock Margin="0,0,5,0">Take a note:</TextBlock>
            <TextBox Text="{Binding Path=CurrentNoteFieldInput}" Width="Auto"/> <!-- TODO: capture input -->
        </DockPanel>
        <UniformGrid Rows="1" Grid.Row="2" HorizontalAlignment="Right">
            <Button HorizontalAlignment="Stretch" Margin="0,0,5,0" Padding="5,3" Command="{Binding SaveNoteCommand}" Content="Save Note" />
            <!-- disabled if no input -->
            <Button HorizontalAlignment="Stretch" Padding="5,3" Command="{Binding ShowNotesCommand}" Content="Show All Notes" />
        </UniformGrid>
    </Grid>
</fw:AcrylicWindow>